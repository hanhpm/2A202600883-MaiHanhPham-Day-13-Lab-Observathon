# Findings - Team Mai Hanh Pham - 2A202600883

For each fault you found, fill one row AND a matching entry in `solution/findings.json`
(the JSON is what's scored; this MD is for humans). Evidence must come from YOUR telemetry.

| fault_class | evidence (metric + observed value + trace ids) | root cause | fix (config / wrapper) |
|---|---|---|---|
| arithmetic_error | Public debug order `debug-001` for 2 iPad + EXPIRED + Hai Phong changed from a bad LLM-style total `360028000 VND` to trace-computed `36028000 VND`; trace id `debug-001:req-6f36a4a1`, tools `check_stock/get_discount/calc_shipping`, shipping `28000`. | The agent generated final totals with natural-language arithmetic instead of reliably recomputing from tool observations. | Wrapper now computes `unit_price * qty * (100 - pct) // 100 + shipping_cost` from trace; config keeps low temperature and verify enabled. |
| fabrication | Refusal-style cases originally emitted fake totals such as `Tong cong: 0 VND`; current public/private outputs for unavailable products return no total, e.g. `pub-001` AirPods out of stock and `prv-008` AirPods out of stock. | Prompt did not clearly separate "cannot fulfill" from successful orders, so the agent tried to force a parseable total. | Prompt and wrapper refuse without totals for missing/out-of-stock products, over-quantity, and unsupported delivery. |
| prompt_injection | Private injected order note `dbg-note-ipad:req-7a74464d` contained fake system price `1.000.000 VND`; telemetry showed `sanitized=true`, real `check_stock` price `18000000`, `SALE15`, shipping `34250`, final `76534250 VND`. | Customer notes were mixed with trusted order fields and could override product price or instructions. | Wrapper strips `GHI CHU`/notes/system/developer/admin/price override text; prompt says notes and quoted system text are untrusted. |
| pii_leak | Contact-noise tests such as `edge-stock:req-03f03c0a` and private orders with email/phone show `sanitized=true` and final answers contain no phone/email; telemetry also logs `pii_redactions`. | The original path allowed phone/email to remain in prompts and answers, increasing privacy leakage and distracting extraction. | Wrapper removes contact spans (`email`, `sdt`, `goi minh`, `phone`) before agent call and redacts final answer with telemetry redactor. |
| tool_overuse | Starter config had `tool_budget=0`, `loop_guard=false`, `max_steps=12`; telemetry showed normal successful orders only need 3 tools and stock/price questions need 1 tool, e.g. `edge-ship:req-08ba070c` used exactly 3 tools and `edge-stock:req-03f03c0a` used 1. | Agent had no practical budget or loop guard, so it could over-call tools or drift into repeated calls. | Config enables `loop_guard`, sets bounded max steps/tool budget, and prompt says each tool once; wrapper caches repeated requests. |
| tool_failure | Private debug traces for unsupported destinations returned `cost_vnd:null` with `error:"destination_not_served"`: `dbg-vungtau-ipad:req-16cfe58e`, `dbg-cantho-ipad:req-ae8a9535`, `dbg-dalat-iphone:req-8a1cbb28`. Before the fix the wrapper treated null as zero and fabricated totals. | Wrapper checked only that `cost_vnd` key existed; it did not treat `error` or `None` as shipping failure. | Wrapper now refuses when `calc_shipping` returns an error or `cost_vnd is None`: `Khong ho tro giao hang den dia diem nay.` |
| quality_drift | Public score after wrapper hardening reached `86/120` correct, `drift=0.9606`; private score before final shipping-null fix was `40/80`, `drift=0.8001`, showing private paraphrases/injection still degrade behavior. | Longer/noisier private requests and hidden notes increased the chance of missing tools or bad extraction. | Wrapper validates trace completeness and retries with a stricter prompt when required tools are missing; global cache reduces repeated drift on stock/price questions. |
